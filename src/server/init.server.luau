--!strict

local RoadService = require(script.Services.RoadService)
local VehicleService = require(script.Services.VehicleService)

RoadService.Start()
VehicleService.Start()

print("[Hoonanga] Server ready")
